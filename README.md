# test_task_3

Функциональные требования:

- [x]  написать функцию, которая детектирует лица на изображении, и возвращает координаты прямоугольников, описывающих
  лицо (на изображении может быть несколько лиц)
- [x]  после обработки функция должна выводить нотификацию о завершении обработки файла (путь) и общем результате (
  сколько лиц нашлось) в консоль
- [x]  организовать threadpool с очередью и несколькими worker, которые будут выполнять функцию детекции лиц с
  параметром - именем файла изображения
- [x]  количество workers должно быть регулируемым, нагружать их равномерно
- [ ]  результат работы функции сохранить в виде графических .jpg файлов с вырезанными найденными лицами, зеркально
  повернутыми по вертикали, а их координаты в исходном изображении и имена файлов результатов, в JSON формате в "
  result.json" файл в той же папке, что и исходное изображение
- [ ]  код собрать в виде dll
- [ ]  сделать консольное приложение для выбора входных данных и их обработки, которое будет использовать dll (загружать
  динамически)
- [ ]  входными данными для приложения будет путь к папке, содержащей подпапки с изображениями (вложенность папок не
  ограничена), и количество workers (может быть не указано)
- [ ]  входные данные передавать как параметры командной строки
- [ ]  Запаковать ресурсные и конфигурационные файлы в один бинарный файл

Общие требования:

- [ ] приложение должно быть собрано также для x86 & x64
- [x] можно использовать opencv, boost
- [x] подразумевается, что компилятор поддерживает C++17
- [ ] код должен быть независимым от платформы (Windows, Linux, MacOs, Android, IOS)
- [x] все комментарии только на английском
- [x] все неописанные моменты - на выбор разработчика

##### (c) Regula Forensics
